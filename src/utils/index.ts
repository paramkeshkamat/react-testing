export const add = (num1: number, num2: number) => {
  return num1 + num2;
};

export const fetchData = async () => {
  try {
    // const response = await fetch()
  } catch (err) {
    console.log(err);
  }
};
